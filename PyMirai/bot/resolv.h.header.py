#pragma once

#include "includes.h"

# struct resolv_entries {
#     ipv4_t *addrs

class resolv_entries(object):
    def __init__(self, addrs):
        self.addrs = addrs
    def ipv4_t(addrs):
        return
    def resolv_lookup():
        return

# class resolv_entries
# struct resolv_entries *resolv_lookup()
